{
  "name": "shagun-home-delivery",
  "private": true,
  "type": "module",
  "description": "Dev tooling only. The published site is plain static files and needs none of this.",
  "scripts": {
    "check": "node tools/check.mjs",
    "test": "node --test tools/",
    "serve": "python3 -m http.server 8765"
  },
  "devDependencies": {
    "jsdom": "^30.0.1"
  }
}
